1、安装Docker


2、Docker 安装 cloudflyer，自由设置 秘钥，记下秘钥。

docker run -itd \
  --name cloudflyer \
  -p 3000:3000 \
  --restart unless-stopped \
  jackzzs/cloudflyer \
  -K 你的客户端密钥 \
  -H 0.0.0.0

3、Docker 安装 flaresolverr，API 调用地址 http://127.0.0.1:8191/v1

docker run -d \
  --name flaresolverr \
  --network host \
  -e LOG_LEVEL=info \
  --restart unless-stopped \
  ghcr.io/flaresolverr/flaresolverr:latest

4、验证：
curl http://localhost:3000   # CloudFreed
curl http://localhost:8191/health  # FlareSolverr

cd到/opt/NodeSeek目录下

安装nodejs
npm install cloudscraper tough-cookie dayjs

创建python虚拟环境

安装python依赖
pip install python-telegram-bot python-dotenv curl-cffi





